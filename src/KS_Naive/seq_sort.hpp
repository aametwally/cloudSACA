#ifndef _SEQ_SORT_H
#define _SEQ_SORT_H

#include "mydefinitions.hpp"

void fastsort_SAtuple(SAtuple* arr, int len);
void fastsort_mapperDuple(mapperDuple* arr, int len);
void fastsort_S(STuple* arr, int nel);
void fastsort_DC3(mapperDuple* arr, int nel);

#endif
